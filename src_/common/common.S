%ifndef __COMMON_S
%define __COMMON_S

; This file's purpose is to make it so the entire common dir gets included with 1 include call
; as none of these files actually have any weight memory wise
%include "defines.S"
%include "macros.S"

%endif
