%ifndef __BIOS_RELOCATE_S
%define __BIOS_RELOCATE_S

[bits 16]

section .text

[global BIOS_relocate]

; Moves data from the source data region to the destination data region
; Inputs:
;   -
; Outputs:
;   - None
;
BIOS_relocate:

    ; TODO

    ret

%endif
